package newChallange;

import java.io.File;
import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.OutputType;
import org.openqa.selenium.TakesScreenshot;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class screenshot {

	public static void main(String[] args) {
		
		WebDriver driver = new ChromeDriver();
		driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
		driver.get("https://demo.nopcommerce.com/");
		driver.manage().window().maximize();
		// 1 full page screenshot 
		//use commad : ctrl+ shift+ o
		/*
		TakesScreenshot ts=(TakesScreenshot)driver;
		File sourcefile = ts.getScreenshotAs(OutputType.FILE);
		// tille here screeshot is captured not we ahve to find where ss is to be captured for 
		// for that new folder is created ss name and new targetfile is created 
		File Targetfile = new File("/Users/architsingh/eclipse-workspace/architproject /Screenshortsfolder//fullpage.png ");
		sourcefile.renameTo(Targetfile);// copy sourcefile to targetfile
		*/
		
		// 2 Specific area ss 
		/*
		WebElement arealoction = driver.findElement(By.xpath("//section[@class='product-grid home-page-product-grid']"));
		File sourcefile = arealoction.getScreenshotAs(OutputType.FILE);
		File Targetfile = new File("/Users/architsingh/eclipse-workspace/architproject /Screenshortsfolder//speificares.png");
		sourcefile.renameTo(Targetfile);
		*/
		
		// 3 Ss of webelement
		
		WebElement logo = driver.findElement(By.xpath("//img[@alt='nopCommerce demo store']"));
		File Sourcefile = logo.getScreenshotAs(OutputType.FILE);
		File Targetfile = new File("/Users/architsingh/eclipse-workspace/architproject /Screenshortsfolder//loog.png");
		Sourcefile.renameTo(Targetfile);
	}

}
